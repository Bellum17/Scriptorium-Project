[phases.setup]
nixPkgs = ['nodejs', 'python3', 'pkg-config', 'cairo', 'pango', 'libjpeg', 'giflib', 'librsvg', 'pixman', 'fontconfig', 'freetype']

[phases.install]
cmds = ['npm ci']

[phases.build]
cmds = ['echo "Build complete"']

[start]
cmd = 'node bot.js'
